# java-stream
java-stream

# Java Stream API
create maven project using command line:
>mvn archetype:generate -DgroupId=com.ayberix.streams -DartifactId=java-stream -DarchetypeArtifactId=maven-archetype-quickstart -DarchetypeVersion=1.5 -DinteractiveMode=false
> cd java-stream
> mvn clean install
